 //        if(isCatLoaded) {if (cat.obj.position.y > -150) cat.obj.position.y -=1;
	// 	else {cat.obj.position.y = 135;
	// }	}

		// //apply a transform to  all objects in scene
		//  scene.traverse(function(obj) {
		// 	 if (obj instanceof THREE.Mesh) 
		// 	 {
		// 		 obj.rotation.x+=0.01;
		// 		 obj.rotation.y+=0.01;
		// 		 obj.rotation.z+=0.01;
		// 		 console.log(obj);
 	// 		}
 	// 	});
